# VPS repair handoff

**Pre-canary update:**8d173cde artifacts are retained as exact historical identity evidence, not an approved candidate. Independent review reproduced unguarded Oura local inference; isolated repair `b164672310d1a3aaa6988532bdecea99b1e96bdc` passes its actual callback/reference tests. One-owner/device intake+baseline admission is also being implemented with additive migration128. Both changes require new integration, tests and matched builds before approval.

Status: combined BLE/VPS implementation frozen at `8d173cdeae3c8af1aee31ff89b927a6b8c05632f`; exact-source tests pass and matched packaging is in progress. Original phone incident remains OPEN. This evidence-only branch does not authorize deployment, installation, model promotion or a main merge. Local, deployed, physical and scientific acceptance are separate.

## Source and shared base

The reviewed common base is `76f2d70f621de91268e295ebcb6c6da29162991f`. Its semantic merge `af19b9b62b65ab0e73f50eb4133e9bf291b0d080` retains both complete histories: integration `e499162b4a0ce340b564e0232af14b5af355d6f1` and PR22 `a972493212f2eae29f01ecaddf9182260153400f`. Integration differs from frozen executable `33a38c5167afec5beeadd700be714e89fa25fb57` only in the release handoff. The actual integration/PR22 divergence has 213/46 unique commits and 42 conflicted paths; the public preview's 34 paths was not the actual merge.

The original dirty checkout at `/Volumes/Untitled/WHOOP NARA-pr16` was preserved. Server work is `repair/vps-server-20260922` in `/Volumes/Untitled/WHOOP NARA-server-repair`. BLE worktree `/Volumes/Untitled/WHOOP NARA-ble-repair`, branch `repair/ble-sync-20260922`, was created at the common base. The user assigned BLE implementation to this team. Reviewed BLE commit `e2cb1f8ab15d302f07f416b8d017ae4b33003ac3` is now merged with VPS `92c337a3168ed224bebfabc271fd4b5dd355b256` at combined `8d173cdeae3c8af1aee31ff89b927a6b8c05632f`, tree `b51ce51ff82e0ba8b6df0f0ab772342258c086b2`. The only merge conflict was resolved to retain both physiological-call and BLE interval exports. See [HANDOFF_ble_repair.md](HANDOFF_ble_repair.md). The authoritative shared ledger is [acceptance.json](docs/server-repair/acceptance.json), with a [readable view](docs/server-repair/ACCEPTANCE_LEDGER.md); the preserved interface and edit ownership are in [STAGE0_COMMON_BASE.md](docs/server-repair/STAGE0_COMMON_BASE.md).

Read: `04_VPS_ALGORITHMS_SPEC.md`, `01_EVIDENCE_AND_DIAGNOSIS.md`, and `/Volumes/Untitled/ble-repair-prerequisites.MhTsik/STAGE0_BLE_INPUTS.md` with its prerequisite reports. The later supplied `/Volumes/Untitled/WHOOP NARA-pr16/FRWHOOP_Recovery_Pack.zip` is now extracted at `/Volumes/Untitled/server-repair-evidence/recovery-pack/FRWHOOP_Recovery_Pack`. All 13 document hashes/lengths match AUDIT_MANIFEST; 01/04 match the Downloads copies. Root read the full shared contract and server/algorithms/lineage evidence; exact contract is retained at [02_SHARED_CONTRACT.md](docs/server-repair/02_SHARED_CONTRACT.md). Independent reviewers reconciled their domains against the full pack. The original missing-directory observation is historical, no longer a blocker.

## Real incident and first broken transition

[Sanitized real trace](docs/server-repair/input_to_result_trace.json) is current hosted server metadata correlated to the historical build 371 SQLite snapshot. It is not a fresh phone capture or normal bearer-authenticated physical readback. The snapshot was read without modifying SQLite/WAL or identity state.

The phone's hosted project is `sgoyxzcagqyxexmsidtk`. At 2026-09-22 23:34–23:38 UTC, a traced accepted batch was ready/indexed and had an exact receipt, but its digest provenance remained client-claimed and later projection debt was pending. There were 119 pending projections from about 01:00 UTC. Thus accepted/indexed input did not establish settled canonical projection. The selected v1 job was pending at revision 612, never claimed, while v2 revision 630 had a shadow publication. The selected sleep, HRV and respiration families still reported awaiting_result. No compatible selected-v1 hosted worker or continuously serviced verifier was observed. The old v1 container targeted VPS-local Supabase, while the phone targeted hosted Supabase. A running shadow worker cannot fulfill that selected queue.

The observed old v1 image was `sha256:27041c79f8cb8e7fdab47eda6d32f6ff49a8232600062105c0da4d6102568590`; old hosted v2 was `sha256:58a843bcc41531c73daa76c7f541ccc5ed04a6f93393574a75f54cae4e84fb04`, source `80a7c0771beff46b60e05ba933e78789b11cdc56`. Missing newer heartbeat fields alone does not prove that an old image never ran; the selected queue and publication evidence establish the routing failure. The six-hour reconcile schedule remains `0 */6 * * *`; it is not continuous service capacity. Current hosted has 117 complete migration identities, 110 native timestamp entries, and lacks the PR22/follow-on migrations. No runtime configuration was changed.

## Implemented repair

- Durable intake now calls the actual atomic projection producer. The forward SQL wrapper retains lifecycle/source/conflict checks. Whole-row JSON conversion avoids accidentally selecting gravity's scalar x field. Exact verified byte proof is attached after immutable object-location transition, so the invalidation trigger cannot erase freshly verified proof. Existing incomplete receipts can be reverified; qualification is never fabricated.
- A dedicated bounded Deno intake service continuously services verification/index, projection and legacy debt through independent serial lanes. Fair scheduling, actual advancing poll state, debt/lease/attempt/rate diagnostics and cleanup are bounded. Async admission requires both explicit enablement and a fresh compatible consumer poll; async remains off by default. Existing pending debt stays recoverable during rollback.
- The baseline rebuild retains the frozen numerical kernel/mapper from `5caa31689da0023e111beb36850d3f81d67e1be2`. Persistent mode ignores stale replay variables; replay requires an explicit argument. No new formulas are hidden under `frwhoop-server-1`.
- Raw model input resolves its immutable acquisition plan before object I/O, pages all required manifests under a short metadata snapshot, and releases SQL before network/inference. Exact-set completeness, source identity, gaps and dedup remain enforced. Limits include 2,048 planned objects, 64 MiB wire/decoded bytes, 100,000 records, 1.3 million samples, 15 MiB serialized input and 120 seconds; incomplete input never invokes the model. Tests include 1/9/257 shards and ingestion during heavy work. This is a bounded implementation limit, not a target-capacity claim.
- Native consumers retain same-scope last-good results, fence retirement/401 and in-flight generations, and poll pending results every 2 seconds for at most 60 seconds while foreground, then resume the normal 15-second interval. Polling is bounded to today/latest explicit day. A real Android auth/publication lock-order regression is covered. Apple task startup no longer cancels its own initial poll.
- Native Test Centre exports include bounded, process-scoped physiological call counters, source/build/policy identity, elapsed coverage and explicit unknown prior-process intervals. The export does not reset counters or identify an owner. Local zero-call tests and physical counter coverage remain separate evidence.
- Worker images, JDBC and PostgreSQL diagnostics retain full hostname/certificate verification and carry the same pinned official Supabase public CA. The migration runner binds its exact path/hash into the plan and revalidates trust before each query. A read-only TLS connection succeeded; no hosted secret or runtime configuration was changed.
- The existing [producer matrix](docs/compute/metric-ownership.json) records 27 families / 80 outputs with separate implementation, wiring, fixture, deployment, input, publication, readback and reference states. Ownership guards and missing envelopes do not count as numerical producer parity. Source-only cutover verification no longer reports production READY.

The atomic intake extension is migration `20260922120000_intake_service_contract.sql`, SHA256 `efb7ea5b812b24ca87f877752dc8f266de6443bdf199c40f869d208e2d051285`. All prior 126 identities/bytes are preserved. The catalog now has 127 entries: 117 applied and 10 pending. Catalog fingerprint `e71489e9317a7a47c1f4c591ca8c26187bf726149e4c456246098673c4d66b24`; integrated schema fingerprint `8c0f6b62fadc0d42fafab8b7ff0140b75198fddbbc728b8b5f2e62ded0e69663`, with 18 functions / 11 triggers / 24 policies / 135 RLS tables. Fresh, populated and predecessor paths passed for the earlier draft, including the actual hosted apply wrapper and unchanged native timestamp ledger. The final predecessor upgrade also passes the new policy-version regression: all 27 prior dispositions are preserved and seven corrected dispositions are appended through the normal worker. The clean92c337a fresh and populated reruns passed with those final migration bytes. The combined8d merge changes no scoring-service or supabase source. Final joined replay still runs the complete chain on8d and will receive its own source-bound receipt.

## Positive numerical proof and limits

The actual joined local chain accepts 22 NDJSON batches through durable WAL, signed loopback object storage, streaming verification/COPY/index receipt and atomic projection. Projection automatically enqueues the selected baseline. The actual persistent baseline executes despite deliberately stale replay variables, publishes immutable results, and the actual enrolled and account handlers agree. Account authentication uses a real disposable GoTrue-issued JWT, with bad signature and wrong-owner rejection. Production Swift and Android decoders/cache selection consume the returned bytes; there is no hand-authored numerical result or approval.

The synthetic input is 10,800 HR samples and 10,800 gravity samples, 01:00:00–03:59:59 UTC, HR 52+floor(n/60)%3 bpm and gravity (0,0,1)g. The unchanged baseline yields 179.98333333333332 sleep minutes, resting HR 53 bpm and canonical sleep efficiency 100%. RR, respiration and oxygen inputs are absent. This proves those existing calculations locally; it does not prove reference sleep, hardware timing, hosted B2, or actual phone rendering. The computed fixture trace remains separate from the sanitized real incident trace. Exact final-source rerun receipts will name the packaged binaries. A separate private real-recording development replay also passed: 109,633 original HR rows, 107,540 gravity rows and 1,044 events were re-encoded into 220 local durable batches; unchanged baseline sleep/resting-HR outputs reached both real authenticated handlers and native persisted selections. Its dirty test source, prior worker binary and prior migration draft are explicitly recorded in `/Volumes/Untitled/server-repair-edge-review/real-recording-development/receipt.json`; it is not final candidate or physical phone acceptance. Patient values remain private.

Raw input qualification still requires actual acquisition evidence. No beat clock was reconstructed from sparse RR, 1 Hz gravity was not relabeled high-rate ACC, provisional temperature conversion was not called calibration, and optical candidates were not converted to SpO2. RR_Estimation's raw production assembler/preprocessing is incomplete; CorrEncoder has no selected pretrained rate producer. Optional models remain shadow. Historical context formulas exist, but canonical family publication/selection and several session/live producers remain engineering work. They are not unsupported-hardware excuses or external-access blockers.

## Deployment boundary and rollback

No deployment/installation approval has been requested or granted. Before requesting it, bind the final source, 127-migration manifest, Edge bundle, intake OCI, baseline OCI, optional undeployed v2 OCI, compiled service configuration and signed phone artifacts; independently review those exact bytes. Shared-contract reconciliation and BLE integration are complete at8d; only artifacts built from that source belong in this recovery release. Keep old release artifacts separately identified; a second copy of a new artifact is not a production rollback.

The observed target has 4 logical CPUs, 7.755 GiB RAM, no swap and 109.21GiB free root disk. Fourteen containers already run. The full template caps total 5 CPUs / 7 GiB before platform services, which is not a safe capacity inference. Initial continuous-service proposal is intake 1 CPU / 2 GiB plus selected-v1 1 CPU / 1 GiB, with history/models/new-v2 excluded; quiescing the old hosted v2 is a separate explicit approval item. Recheck actual peak use before action. The snapshot is idle resource evidence only. The old 1,000-owner local fixture p95~80.75seconds failed the 60-second target and does not establish 1,000-user capacity.

Continuous intake and v1 consumers currently claim all eligible owners in their hosted queues. `intake --once` is also a global claim. A one-owner baseline replay is possible, but it does not isolate a later continuous deployment. The approval must explicitly name the global eligible backlog and selected services, or a separate scoped admission implementation is needed. V2 replay constructs other global retry workers and cannot be called owner-isolated.

After explicit approval: take current read-only ledger/queue/resource and artifact observations; apply only the ten exact additive migrations with the hosted runner and reviewed psql-bound plan; verify schema; start only the reviewed compatible consumers/selected worker and check actual project, source, image, advancing polls and useful publication; deploy the bound Edge roles with async still off; then install the matching phone only if separately approved and without resetting app/owner/pending data. Check normal authenticated readback and displayed immutable revision before a continuity run. Exact service commands, image digests and plan fingerprints belong in the final artifact receipt, not guessed placeholders.

Rollback stops new consumer/worker admission and returns only approved service/Edge versions whose forward-schema compatibility was checked. Keep additive migrations, raw objects, immutable revisions, receipts, local pending data and projection/verification debt. Never down-migrate applied history, clear queues, manufacture acknowledgments, prune pending input or reactivate native inference. Async stays off; if previously enabled later, disable fresh admission while continuing compatible old-debt service. Stop immediately for owner leakage, wrong project/image/source, broken exact-receipt conservation, stale worker overwriting newer output, or resource exhaustion; record the first failed invariant and affected scopes.

## Physical and elapsed acceptance

The latest read-only device inventory lists a paired iPhone 16 on iOS 27.0 build 24A435, but its tunnel is disconnected and DDI unavailable; listing does not prove runnable installed candidate or current-public-iOS coverage. Historical build 371 metadata is not the candidate. Watch metadata similarly does not establish watch-path acceptance. No app launch, install, reset, account change or BLE mode command occurred.

Use the existing Test Centre report and `Tools/SyncAcceptance/manifest.py`. Observe a supported computed result from actual consented input with normal enrolled/account authorization; compare worker input/result revision, API bytes, native cache and visible UI. Capture process-scoped zero-inference evidence and supported numerical output together. Unknown process tails/restarts remain unmeasured. Run four actual locked hours, 24 hours and 72 hours with acquisition/receipt/result continuity, recovery/fault cases, phone resource/energy and queue/CPU/RAM/connections/storage observations. Synthetic clocks, CI, heartbeat or idle host snapshots cannot pass elapsed or capacity gates. Admission cap must come from measured actual bytes/objects/rates and workload mix.

The independent diagnosis/design/implementation reviews are preserved under `docs/server-repair/`; pre-canary artifact review and physical readiness review are separate from actual physical acceptance. The shared contract requires independent review after physical acceptance; no physical review is credited before that run. Current deployment, real phone readback, four-hour continuity, 24/72-hour soaks and target-VPS capacity are all NOT_MEASURED.

## Exact combined local verification

Combined8d full JVM: analytics1,106 / zero failures / five explicit unavailable private/reference fixtures; service590 / zero failures / zero skips, with actual current Swift corpus, disposable PostgreSQL and source-bound installDist. Native verification: capture154 (126 acquisition, four interval metrics, one actual report integration, 23 helpers), Swift focused43, final-hosted36 and Android41 passed. Upload queue99 passed. See the single acceptance ledger for immutable receipts and preserved diagnostic failures.

The actual read-only hosted plan is PLAN_READY, with ten pending migrations, strict pinned public-CA hostname validation, exact psql binary and plan fingerprint `639f842ad2a9c2ae96484176899d8b4c93458f28e39647a024b61d07aca79a1b`. It is not a migration-apply receipt.

A separate isolated branch is implementing remaining Huami/FTMS/Oura exact-notification durability; that follow-up is not in8d phone artifacts. Oldbuild371/33a and commonbase76f cannot safely decode the freshAppend/session journal; rollback requires a compatible forward recovery build and preserved pending bytes. Do not downgrade, uninstall or reset as rollback.
