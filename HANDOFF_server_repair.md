# VPS repair handoff

The original phone incident remains **OPEN**. BLE and VPS repairs are integrated on `repair/vps-server-20260922`; frozen candidate `405ee1b6a6238e7f4c77e2e2f8274d01f524e5f2` has passed the narrow operational candidate checks. Full producer parity remains incomplete. Stale capture/oracle fixtures and native harness comparisons have been corrected with preserved failing regressions; final matched artifacts and independent identity verification passed. Signed iPhone build373 is installed and launched; no registry, hosted schema, Edge/config or VPS worker mutation, model promotion or main merge has occurred.

## Identity and diagnosis

Reviewed common base: `76f2d70f621de91268e295ebcb6c6da29162991f`. It preserves integration `e499162b4a0ce340b564e0232af14b5af355d6f1` and divergent PR22 `a972493212f2eae29f01ecaddf9182260153400f`. The integration tip differs from frozen executable `33a38c5167afec5beeadd700be714e89fa25fb57` only in release documentation. The actual integration/PR22 merge had213/46 unique commits and42 conflicts. The original dirty checkout was preserved. BLE `e2cb1f8ab15d302f07f416b8d017ae4b33003ac3` is integrated, along with later capture, inference-guard and cache repairs.

The complete recovery pack was extracted and all13 hashes verified; its exact [shared contract](docs/server-repair/02_SHARED_CONTRACT.md) is retained. [Stage0](docs/server-repair/STAGE0_COMMON_BASE.md) records lineage and artifact identity. The supplied server/algorithm/lineage evidence and cited source contracts were reviewed. Missing science evidence remains explicit.

The [sanitized original trace](docs/server-repair/input_to_result_trace.json), SHA256 `b6c508bb469b7cde264bc98c382e51c0c4310c21291c12fb9a1be6b5d1ffe6f3`, correlates historical build371 data with bounded hosted metadata. The traced accepted/indexed upload still had canonical projection debt;119 projections were pending. The selected v1 job was pending at revision612 and never claimed, while v2 revision630 had only a shadow publication. The phone used hosted project `sgoyxzcagqyxexmsidtk`; the old v1 worker used VPS-local Supabase. No compatible continuous hosted selected-v1 or intake consumer was observed. This identifies failed projection and selected-worker routing, rather than proving failure from a missing heartbeat alone.

Later read-only admission preparation used that exact installation-authenticated original receipt. It did not select a synthetic replay UUID or an unrelated newest device. The historical SQLite device catalog did not independently match, and its gravity table reported corruption in read-only mode; consequently this is receipt-authoritative historical scope, not current physical BLE linkage. No database/WAL repair or device reset occurred. Post-migration active/retired-source validation remains mandatory.

## Implemented behavior

- Continuous bounded intake independently services verified object/index receipts, projection and legacy debt. Fair fresh/history scheduling, exact byte/digest/location proof, idempotent completion, leases and progressing lane diagnostics are preserved. Projection creates real selected work. Heavy inference runs outside ingestion locks against bounded immutable input plans and fenced revisions.
- Intake and baseline can be restricted to one validated owner/device through SQL128. Atomic claims, publication and completion retain scope and retirement fences. The deployer creates workers stopped with restart disabled. A private guard binds exact image/source/instance/container IDs and owns startup/stop. Its hard budgets and emergency exact-ID stop are tested. Async remains OFF; the existing six-hour reconcile is not a continuous consumer.
- The selected baseline uses unchanged frozen numerical kernel and result mapper from `5caa31689da0023e111beb36850d3f81d67e1be2`. Persistent mode ignores stale replay settings. New algorithms remain distinct and unpromoted. Shadow output cannot satisfy selected-v1 acceptance.
- Every existing legacy RR source lacks independently verified beat timing/continuity: sensor-second/legacy clocks, host-arrival SIG HR, ambiguous old rows and Oura intervals do not establish beat timestamps. Original RR bytes remain archived, while final-hosted native projections and selected-v1 RR inputs are excluded. Independent device HR/gravity still feed existing RHR and heuristic sleep calculations. No beat timing or qualification is fabricated.
- New baseline publications carry the strict `legacy-rr-excluded-1` input-exclusion marker before immutable hashing. This is an input fact, not scientific qualification. SQL129 and both native cache models withhold old unmarked RR-dependent HRV/recovery/respiration and sleep stages/totals while preserving independent HR, episode bounds and in-bed time. Existing immutable payloads/hashes remain unchanged. Native offline caches, nested arrays and responses with the same hash obey the same rule; authorization, revocation, expiry and identity fences remain.
- Apple/Android generic Huami/FTMS/Oura callbacks retain exact bytes with captured owner/device/session/sequence and explicitly unverified host timing. StandardHR exact original receipts remain immutable; a bounded resumable archive upgrade exports previously projected historical occurrences without rewriting original journal bytes or canonical rows. The [BLE handoff](HANDOFF_ble_repair.md) describes durability, ACK and retry changes.
- Native consumers preserve authorized same-scope last-good supported values with age and per-metric missingness. Widgets/watch/exports/Health consumers are covered. Oura local derived HR/sleep paths are guarded. Process-scoped physiological counters expose zero-call evidence and unknown prior-process coverage. Source and local runtime tests cannot prove a physical phone interval.
- Raw model assembly pages the complete immutable required set and releases SQL before object I/O/inference. Limits are2,048 objects,64MiB wire/decoded,100,000 records,1.3million samples,15MiB serialized input and120seconds. Incomplete/gapped/unqualified input abstains before model invocation; this is not measured target capacity.

The [producer/input/output matrix](docs/compute/metric-ownership.json) has27 families/80 outputs and independent implementation, wiring, fixture, deployment, qualified-input, publication, readback and reference states. Canonical history/live/session and generic numerical adapters remain engineering work. RR_Estimation production assembly/preprocessing and a selected CorrEncoder rate producer are incomplete. These are not unsupported-hardware or access excuses. No new model, SpO2, absolute temperature calibration or five-minute beat adapter has been qualified.

## Migration and verification evidence

The catalog now has129 full identities:117 observed applied and12 forward migrations. Its fingerprint is `bd78bdc02131edb3f5de974158202dc948e899cffb85b05970a45b1db2ae4edd`; integrated SQL fingerprint is `d9fb8fcb73324dcc94c67190756c3ec292fec89a2d959ef05bdc3c9915c957f4` with32 functions,11 triggers,24 policies,136 RLS tables and27/80 family/metric definitions. SQL129 SHA256 is `819513ea27f2d6f6fb9f32b317db056262e25abe6af05f6514eb87ade11161b7`. Prior128 identities/bytes are unchanged.

Earlier predecessor fixtures incorrectly synthesized111 native migration rows. Those receipts are preserved as historical and do not prove the actual110-row ledger. The corrected exact hosted110/full117 fixture and12 normal apply wrappers pass, including negative ledger/name mutations and unchanged native history. The independent actual hosted-schema-only dump/restore rehearsal additionally checks captured extension versions, owners, ACLs, RLS, function security and trigger definitions before migration; it passed on final405 with independently reviewed catalog fidelity,110 native entries unchanged and129 full identities after all12 wrappers. Schema-only local restore does not test production patient rows. [Fixture provenance](docs/server-repair/hosted-ledger-fixture.md) states the distinction.

Final405 server tests pass 1,106 analytics tests with five explicit private/reference skips and 590 service tests without skips. The frozen baseline passes 746 analytics tests with the same five reference skips and 33 service tests. Final native validation passes 189 app tests, one reference-mode test, 2,107 Android tests and 17 Swift primitive tests; WhoopStore runs 905 tests with one optional copied-phone database skip. Four exercised hosted paths record zero physiological executions. The [single ledger](docs/server-repair/acceptance.json) binds these runs to their receipts; earlier development slices remain separately identified.

The final405 synthetic joined chain produces actual selected baseline sleep/RHR from 22 durable batches through verification/projection, immutable publication, real GoTrue account and enrolled APIs, and 23 persisted selections in each production Swift/Kotlin cache harness. Eight invalid responses are rejected. A separate private actual 42-hour scalar recording replay preserves 109,633 HR rows, 107,540 gravity rows and 1,044 events in 220 locally re-encoded batches; its final405 rerun passed with explicit baseline replay-day computation, identical enrolled/account output and two persisted native selections per platform. The synthetic fixture separately proves persistent selected-worker polling. Historical8d replay receipts remain preserved without transferring acceptance. The private recording SHA is `7aed414ae54e23d4462dd82b7acb1f75a629948f91f260612d5f07e538846206`; original mobile bytes, actual hosted B2 round trip and physical rendering are not established by that replay. Patient values stay private. The separate [sanitized replay trace](docs/server-repair/real-recording/decoders/real_recording_input_to_result_trace.sanitized.json) and [receipt](docs/server-repair/real-recording/receipt.json) preserve those limitations; neither replaces the original incident trace.

## Proposed deployment and rollback boundary

Final signed iOS373/Android452, all three OCI archives, the migration/Edge bundles and their aggregate manifest have passed independent identity verification. The immutable archive publisher and [matched deployment/rollback plan](docs/server-repair/deployment/review/MATCHED_DEPLOYMENT_APPROVAL_405.md) are independently reviewed and sealed. The [seal](docs/server-repair/deployment/review/approval-plan-seal.json) SHA256 is `816818342fabaa817246eaa569df7a89fa5599b57c8b6b6eb1135470f74e40bf`; the [deployment receipt](docs/server-repair/deployment/deployment-status-receipt.json) explicitly records NOT_PERFORMED. One approval of the complete exact plan covers its enumerated stages; access and stop gates remain mandatory, and changed scope/artifacts require renewed approval. Initial processing scope is the original traced single owner/device, intake1CPU/2GiB plus baseline1CPU/1GiB, with no new history/model worker or model promotion. SQL129 changes legacy read eligibility globally, even though claims/computation are scoped. A bounded actual hosted query observed zero v1 immutable publications and zero legacy daily/night compatibility rows at its timestamp; installed offline caches remain unknown until the matching phone runs.

The target has4CPU/~7.75GiB with existing services. Idle resource observations are not measured capacity. B2 configured-key authorization and signed bucket HEAD succeeded read-only; no body download/write or canary round trip occurred. Registry publication permission and private package visibility remain unverified. Local transport tests preserved all three selected image manifests and 35 config/layer descriptors; they do not prove remote registry or target access. The actual signed app fleet/anon configuration is present, and a strict read-only token-hash check found its fleet token active and unexpired. Usable account and installation bearer credentials are absent from the eight normal parity environment variables and normal build configurations; these normally reside in the phone Keychain. The phone is now connected and build373 runs, but no Keychain extraction or authenticated route request was performed. Enrolled/fleet GET handlers update token-use timestamps, so their first actual route check requires explicit approval too. Target intake.env is absent; a private local file was prepared from the existing normal matching service credential and confirmed with read-only REST. The target dedicated scoring URI currently lacks hostname/certificate verification; an exact private replacement using verify-full and the pinned CA must be included in approved deployment. Existing running containers and credentials are preserved.

The matched order is: obtain explicit approval covering the token-use timestamp writes made by genuine normal authenticated route checks, then prove those routes before registry publication, schema or configuration changes; revalidate source/artifact/target/ledger and pending data; publish all three existing immutable image archives and verify target pulls; apply only the twelve bound additive wrappers and verify; install the exact configuration and matched Edge roles with async off; create only intake and baseline stopped with restart disabled; bind exact container IDs and permit guard-owned startup. Install iOS373 on the original iPhone without reset only within the approved scope and when normal device services work. Then prove useful selected publication and actual authenticated API/cache/UI equality before continuity tests. The final matched deployment plan supplies executable commands, hashes and rollback; unrelated existing containers remain unchanged.

Rollback stops exact candidate containers and fresh admission while preserving additive schema, immutable results, receipts, raw objects, debts and phone journals. Keep read eligibility gates and use a forward-compatible signed recovery phone. Older371/372/common-base binaries cannot safely resume all current freshAppend/transport/archive state; no downgrade, uninstall, reset or cursor deletion. The old local-DB worker is not a hosted rollback. Async remains off; old hosted shadow and local containers are untouched unless separately authorized. Stop for wrong project/source/owner, broken exact receipt, stale publication, failed retirement fences or resource budgets.

## Physical acceptance and remaining gates

At the earlier read-only inventory, the paired iPhone16 tunnel was disconnected and DDI unavailable. On 2026-09-23 the tunnel connected, DDI became available, and signed iPhone build373 installed in place over build371 and launched. Main-app and widget processes were observed. No uninstall, phone reset, enrollment reset or BLE control command was issued; pre/post app data-container identity was not captured. The signed Watch profiles omit the observed paired Watch and no Watch installation or configuration change occurred. Android452 still has no identified physical installation target. Installation and process presence do not prove server result readback or physical zero-local-inference behavior.

Required actual acceptance: supported computation from consented wearable input, immutable publication and normal enrolled/account read matching cache/UI, zero local physiological counters over complete process intervals, foreground latency and at least four elapsed locked hours,24/72-hour recovery/energy/continuity soaks, and bounded target-VPS mixed-workload capacity. Report p50/p95/p99/max/count/failures/timeouts/unfinished/missingness and total OS deferral. None is passed by CI, a heartbeat or local replay. The1,000-user claim remains unsupported. Physical, scientific and general production acceptance stay separate from a narrow operational canary.
