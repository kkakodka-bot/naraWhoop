The matched source is `405ee1b6a6238e7f4c77e2e2f8274d01f524e5f2`. The real signed phone artifacts, three OCI archives, migration/Edge bundles and private worker plan have passed aggregate/offline verification. This is a deployment approval proposal; nothing has been published, applied, started or installed. The exact immutable-archive publisher and official pinned tool passed local digest-conservation and independent review. Normal route credentials, private registry permissions and physical phone access remain separate access prerequisites.

The proposed continuous deployment has exactly two new processes: the intake consumer and selected frozen-v1 worker, limited by database-enforced admission to the original authenticated trace owner/device pair. The identifiers remain in `/Volumes/Untitled/server-repair-private/deployment/admission.json`; its canonical fingerprint is `77ae1403816bc64b0f71ab2b79241c5ffd39c5baf1ce4100106b8f7f2358e05e`. Historical phone evidence and the authenticated original receipt establish this proposed pair; current ownership and active state were checked read-only. Physical radio/device linkage still needs actual phone verification. The old local snapshot's device catalogue did not independently match the server receipt device, and its gravity-table read reported corruption. That snapshot was not repaired or reset.

SQL129 changes legacy-v1 read eligibility globally, including readers outside the canary pair. It preserves immutable historical payloads and masks beat-dependent values without qualified timing. Updated native consumers also mask old offline caches. Valid independent resting HR and session bounds remain; newly marked RR-excluded v1 publications may expose their supported heuristic sleep results. This is not physiological qualification or a new model promotion. The bounded hosted observation at 2026-09-23T05:30:46Z found zero stored immutable-v1 publications and zero compatibility day/night rows; installed offline caches remain unknown. These observations do not narrow the global policy's scope.

All existing containers retain their identities, configuration and running states. The preflight found no hosted selected-v1 container and no intake container. Abort if either appears or the inventory changes before execution; this proposal does not authorize the deploy script's generic replacement of an unexpected existing hosted-v1 worker. The existing old v2 and self-hosted baseline are outside the two-container guard. No new persistent v2/history/model process is proposed. Deployment performs a bounded transient v2 configuration check, using the matched v2 image without promoting it.

Async verification admission remains off. The prepared intake configuration explicitly sets `NOOP_ASYNC_OBJECT_VERIFICATION=0`; the scoped consumer cannot make global async admission ready. The approved Edge configuration must also explicitly remain off. The six-hour reconciliation path is not enabled as a substitute for the continuously serviced verifier/projection queues.

The target has four logical CPUs and approximately 7.75GiB RAM. New limits are intake 1CPU/2GiB and baseline 1CPU/1GiB; the transient v2 check is 1CPU/1GiB. Thirteen other services were uncapped at the read-only inventory. These are proposed operational limits, not measured capacity. The 1,000-user claim remains unsupported.

All paths below are literal reviewed locations or variables that must be populated from the final signed receipts. No credentials or private owner/device identifiers should be echoed, placed in command arguments, or copied into public receipts. The deployer-required ignored key path in the clean 405 repository now contains a 0600 regular copy of the original normal key; its public fingerprint matches the bound plan, the original is preserved and no key was rotated. Do not run any mutation block until explicit approval covers its exact source, fingerprints and target. A new approval is required for scope expansion or a changed artifact. Approval of this complete plan covers its enumerated exact stages, including guarded activation and installation on the original iPhone; no repeated confirmation is required while the approved artifacts and scope remain unchanged. Every credential, identity, resource, registry-visibility and physical-access prerequisite and stop condition still applies. Approval never implies that a test passed.

The aggregate file SHA256 is `9a6f857574fa1019f69013ec912cabc54ab0d59b4baaf755e5df8ced493b1b0c` (manifest fingerprint `9c22456cabbab3196b6472ad2f0d67de004a930a0d6b86618ade7fc82fa0aeff`). The hosted migration-plan fingerprint is `21243b3e4d1a17b974289478806c191fb71309a6eed86b85c0f632433539fce6`. Exact approved inputs:

| Artifact | SHA256 or OCI manifest digest |
|---|---|
| Selected v1 OCI | `sha256:aa69800d726b3c9d6e0fac617903035d1a39d99c2f2000bc96c0b66e86d17dbe` |
| V2 OCI for transient check | `sha256:875e81ef8072cf449475c38d3b22259853ec45c67498b50ef4801a46eae5747e` |
| Intake contract2 OCI | `sha256:0db2cab4d193034225e6d7f067e4b2beaa368d65785ecb9c73857de131e1030c` |
| iOS373 IPA | `09b9ec446de52ec91d7eb28a03b24a19f7ddf9da6401bc09be8c06bb60b9e8cf` |
| Android452 APK; no install target | `784514b706bf430f408a77934df4904f7ccabe72972e40790f6caf4fd72bff20` |
| 129-entry migration manifest | `a4e5ab76ff10379895d5c3bf64922f3582cde84951a7d38fb7e1301e14031a68` |
| Six-function Edge archive | `750cee57864858a4d9d8a13035ade61d9ef94b58b76d7ee5031520a05c2e7d40` |

The proposed registry references, configuration digests, full archive hashes, exact TLS helper/update hashes, admission hash, compiled Compose hash and six guard-policy/dependency hashes are in the sanitized JSON; owner/device IDs and normal credentials remain private. The compiled intake Compose hash is `fd7472ec86df40fb96f6d22b2f17ddcbb1a96d00acb50b36712717c55236fd97`.

1. Review the completed source/artifact binding and remaining access prerequisites.

   These non-secret command variables resolve to actual reviewed artifacts:

   ```sh
   REPAIR_REPO='/Volumes/Untitled/WHOOP NARA-server-repair'
   REPAIR_RELEASE='/Volumes/External SSD/server-repair-release-405ee1b6a6238e7f4c77e2e2f8274d01f524e5f2'
   REPAIR_RELEASE_MANIFEST="$REPAIR_RELEASE/release/release-artifact-manifest.json"
   REPAIR_EDGE_BUNDLE="$REPAIR_RELEASE/edge/source-bundle"
   REPAIR_EDGE_BUNDLE_SHA=750cee57864858a4d9d8a13035ade61d9ef94b58b76d7ee5031520a05c2e7d40
   REPAIR_SUPABASE_CLI=/opt/homebrew/Cellar/supabase/2.75.0/bin/supabase
   REPAIR_SUPABASE_CLI_VERSION=2.75.0
   REPAIR_SUPABASE_CLI_SHA=e4c3a5d90e4ebb1782459a50d5cedac2fc6512c9cdc228bf73d80deea34d3c43
   export JAVA_HOME=/opt/homebrew/opt/openjdk@17/libexec/openjdk.jdk/Contents/Home
   export PATH="$JAVA_HOME/bin:$PATH"
   export DOCKER_CONTEXT=colima-frwhoop-integration
   cd "$REPAIR_REPO"
   ```



   Use the final release root `/Volumes/External SSD/server-repair-release-405ee1b6a6238e7f4c77e2e2f8274d01f524e5f2`. The private input is now populated from actual artifacts; the following offline binder already passed and produced the private0600 worker plan. Its output fingerprint is `cb938cae5377abb026bbcae8b956d82cfcd679045ba7b1590e7a93eff81e9746`; `verify-deployment` independently passed. Do not recreate the plan or instance identity unless a reviewed input changes:

   ```sh
   python3 "$REPAIR_RELEASE/deployment/review/bind-final-worker-plan.py" \
     '/Volumes/Untitled/server-repair-private/deployment/worker-deployment.inputs.pending.json'
   ```

   It requires clean 405, exact reviewed source-contract bytes, the canonical private admission fingerprint, and the real aggregate artifacts. It wrote a private0600 executable worker plan using the repository's actual `bind-deployment` implementation. Its pending input is not an executable worker plan. The corresponding guard input remains a skeleton until actual stopped container IDs exist.

   The actual read-only hosted migration plan is already bound to405: fingerprint `21243b3e4d1a17b974289478806c191fb71309a6eed86b85c0f632433539fce6`, under `hosted-readonly-plan/`. It observed the real110 native ledger rows and117 full migration identities, with12 forward migrations. Actual hosted schema-only capture, exact security semantics, and all12 production wrappers passed locally at405; no patient/application rows were restored. Preserve this limitation in the approval request.

   Actual OCI manifest/config digests, signed iOS373/Android452, migration/Edge bundles, CLI identity, private worker-plan fingerprint and target SSH pins are bound in `matched-deployment-plan.json`. Genuine account JWT and enrolled-bearer availability still must be resolved. The actual signed405 IPA supplies the normal fleet and anon keys; a bounded strict-TLS read-only SQL lookup proves its fleet token is explicitly fleet-scoped, active and unexpired. The original scope has a current installation-token hash, but no recoverable plaintext bearer column. Registry read/write permissions remain NOT_VERIFIED. Normal pinned SSH was checked read-only: the root login has effective UID0, the deploy login UID1000, `systemctl` is present and PID1 is systemd. No supervisor command was executed. Normal hosted DB and service-role configuration and the selected B2 bucket were verified read-only; that does not establish registry permissions. The eight named operator environment variables and normal original/final build configs contain no account JWT or installation bearer. Those credentials normally reside in the original phone's ThisDeviceOnly Keychain, which was not accessed; missing material cannot be labeled expired. Restore normal usable access to that enrolled phone/account, or supply the matching existing credentials through a private operator channel. Do not scan unrelated Keychains, reset enrollment or substitute service-role credentials. The FIRST explicitly approved API action must prove both existing authenticated routes for the intended scope, before registry/DDL/config. Enrolled/fleet authorization deliberately updates token `last_used_at`; approval must cover those usage-timestamp writes, so this GET is not called read-only and has not been executed.

2. After approval, refresh target identity, inventory and admission prerequisites before any mutation.

   Use the normal deploy key and strict known-host file from the bound target identity receipt. Do not use accept-new, host-key bypasses or a different target. Capture only bounded metadata: exact old container IDs/images/source/config hashes/status, no existing hosted-v1 or intake candidate, scoped backlog and oldest age, host memory/disk, and database connection totals. Abort on drift or failed resource thresholds. Keep the prepared source identity and original authenticated pair; do not replace them with a most-recent unrelated device. Revalidate the active canonical pair with the new canary RPC after migrations, before either worker starts.

3. Prove normal authenticated routes, then publish and verify all three immutable image archives before hosted schema mutation.

   First perform the explicitly approved normal account/enrolled GET proof with the actual same-scope runtime credentials. Record only status/scope checks and response hashes; its normal token-usage timestamp updates are included in approval. Missing credentials or failed proof stops the plan before any registry, DDL or configuration changes. The post-Edge deployed-source parity verification later is still mandatory.

   Explicit approval must include publication of the already-built baseline, physiology and intake archives to the proposed `ghcr.io/kkakodka-bot/frwhoop_v2/{baseline,physiology,intake}` repositories. Before pushing source-bearing images, use the normal authorized registry access to establish that all destination packages are private and approved. Public or unverifiable package visibility stops publication pending explicit additional scope approval. No public release is authorized. Registry credentials, read/write permission, private visibility, digest-only acceptance and exact target pull availability have not been established; no GHCR request occurred during preparation. These gates precede hosted SQL or configuration changes.

   The reviewed publisher copies existing archives without rebuilding. It uses official Skopeo1.22.3 pinned to `quay.io/skopeo/stable@sha256:3f276c7780973ede33a7ae4a5f5ac002cb2fd53d6d892f38ab313def3570188d`. That is the linux/arm64 tool image; payload selection is explicitly linux/amd64. `--preserve-digests` and digest-only destinations preserve the reviewed manifest/config bytes. The final local disposable-registry round trip retained all three selected manifests and35 config/layer descriptors, independently rehashed original archives and returned source indexes404 with no mutable tags. Fourteen helper tests passed. This proves local transport, not GHCR publication or target pulls.

   The publisher source SHA256 is `4ea6213200be7a94e646ece25cb3048c0fde65f3e9a132c701634cfe3d2d2d45`; its plan SHA256 is `5a9070e0a3ebb9236fa7f1e5f96f5e3d177fa49e1b9b8c3432362ab9711a4941`. Final transport receipt SHA256 is `d8eeb6dd2bab2e43969617f2e792cbf81492a2e7a2a98f70ac4483189f662b5f` and independent review SHA256 is `3cb6cbff69b968ce164c5f4be33940999bc05fc5f22c547db92689d9c406532b`. Their canonical paths are under `verification/oci-publication/`; byte-identical public operator copies accompany this plan.

   After all these gates and explicit approval, set `REPAIR_GHCR_AUTH_FILE` to a canonical operator-owned0600 file from the normal authorized registry session. It must contain only the GHCR inline auth entry, no helpers or unrelated registries. Do not print its content or put a token in argv/history. The existing shared staging directory must be operator-owned0700 with at least1GiB available. The concrete approved publication command is:

   ```sh
   python3 "$REPAIR_RELEASE/verification/oci-publication/publish-approved-oci.py" \
     --plan "$REPAIR_RELEASE/verification/oci-publication/publication-plan.json" \
     --release-root "$REPAIR_RELEASE" \
     --evidence-dir "$REPAIR_RELEASE/verification/oci-publication/approved-registry-publication" \
     --execute \
     --approved-plan-sha256 5a9070e0a3ebb9236fa7f1e5f96f5e3d177fa49e1b9b8c3432362ab9711a4941 \
     --auth-file "$REPAIR_GHCR_AUTH_FILE" \
     --docker-host unix:///Users/rahulvijayan/.colima/frwhoop-integration/docker.sock \
     --docker-staging-dir /Users/rahulvijayan/.cache/frwhoop-oci-publication-405
   ```

   The evidence directory must be new. Do not pass the local-registry test flag during remote execution. Remote TLS verification is required. The helper reads back the remote manifest/config/layer bytes; target pull access must also be verified before SQL. If any image fails, stop with the partial immutable-publication receipt and leave hosted schema untouched. Do not substitute the existing v2 rebuilding publisher, convert/rebuild images, add mutable tags or change approved digests.

   Publication of the physiology image authorizes only the matched transient configuration check in this initial plan. It does not authorize a persistent v2/history process, selected model promotion or all-owner processing. Only the stopped intake and baseline containers will be eligible for later guarded start.

4. Apply the twelve matched hosted migrations and verify them.

   The operator supplies `FRWHOOP_HOSTED_DATABASE_URL` in process memory using the already reviewed normal-config reader and strict pinned CA. It is never a command-line argument. From the clean 405 repository, the exact mutation is:

   ```sh
   node Tools/release/hosted-migration-release.mjs apply \
     --repo-root "$REPAIR_REPO" --manifest "$REPAIR_RELEASE/migrations/migration-manifest.json" \
     --project-ref sgoyxzcagqyxexmsidtk \
     --target-binding "$REPAIR_RELEASE/hosted-readonly-plan/hosted-target-binding.json" \
     --plan "$REPAIR_RELEASE/hosted-readonly-plan/hosted-migration-plan.json" \
     --apply-plan-fingerprint 21243b3e4d1a17b974289478806c191fb71309a6eed86b85c0f632433539fce6 \
     --evidence-dir "$REPAIR_RELEASE/hosted-readonly-plan" \
     --psql-path /opt/homebrew/Cellar/postgresql@18/18.3/bin/psql
   node Tools/release/hosted-migration-release.mjs verify \
     --repo-root "$REPAIR_REPO" --manifest "$REPAIR_RELEASE/migrations/migration-manifest.json" \
     --project-ref sgoyxzcagqyxexmsidtk \
     --target-binding "$REPAIR_RELEASE/hosted-readonly-plan/hosted-target-binding.json" \
     --plan "$REPAIR_RELEASE/hosted-readonly-plan/hosted-migration-plan.json" \
     --evidence-dir "$REPAIR_RELEASE/hosted-readonly-plan" \
     --psql-path /opt/homebrew/Cellar/postgresql@18/18.3/bin/psql
   ```

   The wrapper checks immutable source identities, actual native ledger and supersession rules before each transaction. Expected final totals are129 full identities with the original110 native ledger rows unchanged. The expected selected-schema fingerprint is `d9fb8fcb73324dcc94c67190756c3ec292fec89a2d959ef05bdc3c9915c957f4` (32 selected functions,11 triggers,24 policies). Any mismatch stops the plan; no force, edited ledger or replay of old migrations is authorized.

5. Install the approved exact configuration and publish the matched Edge bundle.

   As the existing deploy/config owner UID1000, transfer the prepared0600 `intake.env` to its previously absent `/opt/frwhoop/intake.env` with exclusive creation, preserving the normal B2 file. Compare the full local/remote hashes without printing content. The prepared file contains the existing normal hosted key, explicit verified bucket, and async off. Existing different content is a drift blocker, not permission to overwrite it.

   Install the committed public CA at `/opt/frwhoop/supabase-prod-ca-2021.crt` only if absent, or require existing bytes to match SHA256 `700723581420dd1ac98fd7e9ac529f0ef210eadcaf87fc868a3ad7d114c2f3b7`. Install the reviewed external TLS helper and private update file into a private staging directory as the existing config owner. The only shared-config change is `SCORING_DATABASE_URL`: retain credential, host, database and other options, replace `sslmode=require` with `verify-full`, and append the pinned CA path.

   ```sh
   python3 /opt/frwhoop/prepared/apply-scoring-tls-update.py \
     --update /opt/frwhoop/prepared/scoring-database-tls-update.json
   python3 /opt/frwhoop/prepared/apply-scoring-tls-update.py \
     --update /opt/frwhoop/prepared/scoring-database-tls-update.json --execute
   ```

   Compare helper/update hashes against the preparation receipt before invocation. The helper requires the exact old file and value hashes, preserves every other configuration line, writes an exclusive original backup, fsyncs both backup and directory before atomically replacing the live file, and emits hashes only. Nine synthetic tests and an independent old-behavior fault regression passed. It has not run against the target. The same CA path exists inside the worker images and is mounted read-only by the reviewed database diagnostic client. Actual strict-TLS read-only access already passed locally with the same credentials/CA; the deployer repeats target-side project/TLS validation before baseline execution.

   Set only `NOOP_ASYNC_OBJECT_VERIFICATION=0` in the hosted project's approved Edge configuration, using `/opt/homebrew/Cellar/supabase/2.75.0/bin/supabase` (version2.75.0, SHA256 `e4c3a5d90e4ebb1782459a50d5cedac2fc6512c9cdc228bf73d80deea34d3c43`) and the normal authenticated session. Do not enable optional async admission. Deploy the exact source bundle and all six matched functions through the reviewed tool:

   ```sh
   node infra/vps/scripts/deploy-hosted-edge-functions.mjs deploy \
     --project-ref sgoyxzcagqyxexmsidtk --repo-root "$REPAIR_REPO" --artifact-root "$REPAIR_RELEASE" \
     --release-manifest "$REPAIR_RELEASE_MANIFEST" --bundle "$REPAIR_EDGE_BUNDLE" \
     --expected-bundle-sha "$REPAIR_EDGE_BUNDLE_SHA" \
     --functions account-deletion,ingest-verify,push,reconcile,retention-sweep,scores \
     --supabase-cli "$REPAIR_SUPABASE_CLI" --expected-cli-version "$REPAIR_SUPABASE_CLI_VERSION" \
     --expected-cli-sha "$REPAIR_SUPABASE_CLI_SHA" \
     --parity-command "$REPAIR_REPO/infra/vps/scripts/verify-hosted-score-route-parity.mjs" \
     --receipt "$REPAIR_RELEASE/hosted-edge-deployment.json"
   ```

   The tool requires genuine normal account/enrollment/fleet credentials in memory and verifies source-bundle deployment plus account/enrolled route parity. Missing credentials stop deployment; service-role-only reads do not replace those checks. Do not fabricate a token or reset enrollment to make the check pass.

6. Stage both exact workers stopped; preserve old containers.

   Approved registry publication/availability of all three digest-pinned images is a prerequisite. No mutable tag substitutes for a digest. Repeat the exact no-hosted-v1/no-intake inventory immediately before invoking this command and abort on any change. The generic deployer can replace a matching existing hosted-v1 process; the initial scope flag alone does not enforce its absence. That replacement is outside this approval. Run from the clean 405 repository with the Java/Docker environment above:

   ```sh
   infra/vps/scripts/deploy-scoring-service.sh \
     --release-manifest "$REPAIR_RELEASE_MANIFEST" --artifact-root "$REPAIR_RELEASE" \
     --worker-deployment '/Volumes/Untitled/server-repair-private/deployment/worker-deployment.json'
   ```

   This validates source/artifacts/target pins, pulls and verifies OCI bytes, checks exact scope and image commands, and stages intake then baseline stopped with `restart=no` from creation. It reports `INTAKE_STAGED_NOT_STARTED` and `CANARY_STAGED_NOT_STARTED`. Preserve the private remote build path and deployment lock. Capture the exact full container IDs from inspection and verify both remain stopped, correctly scoped and resource-limited. No manual `docker start`, restart policy change or unguarded worker command is authorized.

7. Bind and activate the supervised canary only after the stopped state is reviewed.

   Copy these exact405 files into `/opt/frwhoop/canary/`, along with root-owned0600 `deployment.json`: `scoped-canary-guard.py`, `scoped-canary-stop-policy.json`, `frwhoop-scoped-canary.service`, `scoring-admission.py`, `verify-worker-image.py`, and `verify-pinned-postgres-client.py`. The private plan binds all six source hashes; install the service file beside the helper as well as under `/etc/systemd/system/`. Do not edit the copied policy or unit. Use the verified normal root SSH session for this phase. Install the unit from its plan-bound copy and bind as the same root identity that will run the service:

   ```sh
   python3 /opt/frwhoop/canary/scoped-canary-guard.py bind \
     --plan /opt/frwhoop/canary/deployment.json --policy /opt/frwhoop/canary/scoped-canary-stop-policy.json \
     --binding /opt/frwhoop/canary/binding.json \
     --intake-container "$REPAIR_EXACT_INTAKE_ID" --baseline-container "$REPAIR_EXACT_BASELINE_ID"
   python3 /opt/frwhoop/canary/scoped-canary-guard.py validate \
     --plan /opt/frwhoop/canary/deployment.json --policy /opt/frwhoop/canary/scoped-canary-stop-policy.json \
     --binding /opt/frwhoop/canary/binding.json
   install -o root -g root -m 644 /opt/frwhoop/canary/frwhoop-scoped-canary.service \
     /etc/systemd/system/frwhoop-scoped-canary.service
   systemctl daemon-reload
   systemctl start frwhoop-scoped-canary.service
   ```

   Binding verifies actual saved-image config bytes, immutable engine image IDs, source, exact commands, pair, consumer contract2, limits and stopped/restart-disabled state. Service startup arms cleanup before starting only those two IDs. The guard checks real scoped poll/work/debt evidence, resources and DB connection counts, not a heartbeat alone. Polls are5s; failed/stale observations, >30s lane age after60s startup grace, >1,000 queued jobs, >120s debt stall, >90% container memory, >80% DB connection usage, <1GiB host available RAM or <10GiB disk trigger stop. These conservative stops do not prove capacity or physical latency. `ExecStopPost` handles supervisor termination; `Restart=no` prevents silent reactivation.

8. Perform physical acceptance as a distinct stage of the approved complete plan.

   The initial phone-install approval scope is only the observed original iPhone16 with matched signed iOS373 and the traced WHOOP pair, after usable normal device services and approval of this complete plan. Android452 is built and tested but has no identified physical install target; no Android installation is proposed. The observed Watch is not covered by the current signing profile and also has Developer Mode off/DDI unavailable; no profile registration, mode change or Watch installation is authorized. IPA373 embeds a Watch companion. Normal iPhone-only installation may reject the package or require companion provisioning/installation. If that occurs, stop: do not strip, re-sign or repackage the IPA, register a device, change Watch Developer Mode, reset either device or expand this approval. Artifact identity and signing checks do not establish that the iPhone installation will succeed. Preserve app data, owner/source enrollment, archives and pending selections. Verify actual server publication → normal authenticated phone fetch → native cache/decoder/UI for the original selected pair, including source/image/result revisions and explicit missingness. Compare supported RHR/sleep results and zero local physiological-call counters with actual device logs. Do not substitute a local decoder harness or synthetic fixture for this evidence.

   First attempt a separately recorded10-minute supervised positive readback to establish actual supported publication and visible phone values with zero-local-call evidence; this short result cannot pass continuity, energy or soak gates. The current iPhone tunnel/DDI is unavailable and its15-second details query timed out. The exact instrumented protocol, conservative resource/latency targets, baseline requirements and missingness are preserved in `verification/physical-readiness/HANDOFF_physical_review.md` under the final release root. Those are proposed engineering budgets, not observations. Then measure at least four hours locked continuity and the requested24/72-hour soaks with actual acquisition, durable ingestion, verifier/projection progress, computation/publication and phone readback. Retain failure, timeout and missing-opportunity denominators. Measure target-VPS workload, delay and resource behavior separately. All these gates are currently NOT_MEASURED. A1000-user or general production-readiness claim is not permitted from a one-pair canary.

Rollback stops only the two bound canary IDs and preserves accepted input/debt/results. The ordinary stop command is `systemctl stop frwhoop-scoped-canary.service` in the verified root session; emergency cleanup uses the exact bound `scoped-canary-guard.py stop --execute` with its private plan/binding/policy paths. Stop identity checks use immutable container ID and image, so configuration drift cannot prevent stopping the correct candidate. No name-only stop, unrelated container removal or queue reset is proposed.

Keep additive migrations and immutable historical data. Do not reverse SQL129 to re-expose unqualified values. Do not automatically restart an old unfenced worker or restore a stale Edge bundle. Any compatible rollback artifact requires its own source/digest/schema review and approval. The previous shared configuration is preserved in the TLS backup and deploy rollback directory; restoring `sslmode=require` would fail the new strict deployment contract and is not an acceptable operational rollback. If the new process cannot run, leave it stopped while repairing forward. Retain guard logs, private bindings and deployment lock until recovery is reconciled; release a lock only after its owner token and exact stopped state are reviewed.

The accompanying `matched-deployment-plan.json` and final approval-plan seal bind the actual artifacts and this material. Publication-tool proof is complete locally; normal credential/private-registry/device access prerequisites and explicit approval remain open. The implementation and local evidence do not establish that the original physical-phone problem is fixed.
