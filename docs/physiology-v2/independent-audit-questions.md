# Required audit questions: evidence and boundaries

Scope: repaired worktree `/Volumes/Untitled/WHOOP NARA-physiology-audit`, compared with original implementation `5250912e5647108c548afa31b048ee4f4f6df133` and baseline `5caa31689da0023e111beb36850d3f81d67e1be2`. This is an evidence index, not a claim that every possible failure was excluded. Source paths below are repository-relative. Final ending SHA and aggregate test totals belong to the lead report.

Path abbreviations: **Swift analytics** = `Packages/StrandAnalytics/{Sources/StrandAnalytics,Tests/StrandAnalyticsTests}`; **Swift store** = `Packages/WhoopStore/{Sources/WhoopStore,Tests/WhoopStoreTests}`; **Android analytics** = `android/app/src/{main/java/com/noop/analytics,test/java/com/noop/analytics}`; **service** = `scoring-service/service/src/{main,test}/kotlin/com/frwhoop/scoring`. A test named below is a source-test reference; execution evidence is identified in the final section rather than implying a separate independent invocation of every method.

## 1. Was PR 15's PPG identity repair reconciled without regressing PR 16?

**Verified in schema, writer and fixtures, with a private-device-copy gap.** Swift `Database.swift:955-1000` retains PR 16's `v46-rr-source-index` and `v47-server-score-cache`, and adds `v48-ppg-record-identity`. Legacy rows retain `rowid`, sample bytes and a sentinel `recordIndex=-1`; an already-widened `(deviceId,ts,recordIndex)` schema is accepted after checking its primary key. Android's `WhoopDatabase.kt` migration 39→40 has corresponding record identity handling; later R-R provenance migration remains separate. The repaired `StreamStore.swift` also removes timestamp-frontier suppression that otherwise silently discarded absent older/same-second records.

Tests: Swift store `PpgSchemaCompatibilityTests.testLegacyMigrationPreservesBytesAndRowIDsUsedByUploadCursors`, `testResearchSchemaReopensWithoutLosingSameSecondRecords`; Android `data/PpgRecordIdentityMigrationTest.migrationPreservesRowIdsBytesAndMultipleRecordsPerSecond`; Swift store `BackfillFrontierTests.testEveryLegacyFrontierAllowsAbsentOlderRowsAndReplayKeepsDebtToken`. The private copied-phone database case is explicitly skipped when its external fixture is absent; do not count it as passed. Preservation of PR 16 migrations does not constitute a physical BLE/backfill regression test.

## 2. Do distinct same-second PPG records survive storage, replay, upload and scoring?

**Verified across local storage, binary serialization and the server raw reader; no live end-to-end device/cloud/model run.** `StreamStore.swift` uses wire record identity in the natural key. Swift store `BackfillFrontierTests.testSameSecondDistinctAndEqualOccurrenceIdentitiesSurviveAcrossChunks`, `PpgWaveformSampleTests.testPpgWaveformPrimaryKeyIncludesWireRecordIdentity` and `testPpgWaveformInsertRoundTripAndDedup` preserve separate records while rejecting duplicate replay. `Packages/NoopPush/Tests/NoopPushTests/PpgIdentityBinaryTests.testSharedOraclePreservesMultipleRecordsAndUnsignedIdentity` and Android `push/PpgIdentityBinaryTest` consume the shared serialized identity fixture; `testLegacyV1StillDecodesAndRetainsOriginalBytes` retains legacy compatibility.

Service `VerifiedRawObjectReaderTest.gzipDigestUsesUncompressedContractAndPreservesSameSecondIdentity`, `zstdAndLegacyDecodeWithoutInventedIndex`, and `signedColumnViewPreservesEveryI16ValueAndIndependentIndexedAccess` verify identity after server decoding. Portable iOS-selected Zstandard output was independently decoded by both the reference CLI and JVM zstd-jni with unchanged shared object hashes. This proves transport/reader seams using real serialized fixtures, not a physical iPhone upload or actual PPG checkpoint execution. Raw waveform availability is not evidence that an unqualified model contributed a canonical score.

## 3. Do native and server R-R readers select identical canonical data?

**Shared-window policy and identity parity are tested.** Swift `Whoop5RRReadPolicy.swift`/`Reads.swift`, Android repository/DAO reads, and service `db/CanonicalRrPolicy.kt`/`SignalSampleReader.kt` use the family/source policy; WHOOP 5 history takes precedence over eligible standard-HR fallback, without mixing unsupported realtime or legacy rows into the selected train. Stored milliseconds are not reconverted from ticks. Swift store `CanonicalRrSourceOracleTests.testCanonicalNativeReadMatchesSharedServerFixture` compares timestamps, milliseconds, occurrence sequence, ordinal and channel to `canonical_rr_source_oracle.json`; service `CanonicalRrPolicyTest.sharedNativeStoreSourceOracle` consumes the same cases. `testPacketToStoreReplayKeepsDecodedUnitsAndSource` and `packetStoreServerOracleDoesNotReconvertTicks` cover the serialized packet/store/server seam.

R-R metadata promotion is now visible beyond a saved append cursor: Swift `RrPromotionExportTests.testMetadataPromotionCrossesSavedAppendCursorAndCreatesDebtWithoutAddingBeats` and `testPromotionAfterDeletingHighestRowInvalidatesReusedCursorIdentity`, plus Android `data/Whoop5RRSqliteTest`. These checks do not assert identical data when callers request different windows, nor validate unknown firmware clocks.

## 4. Can missing or suspect intervals create false adjacency?

**Repaired/new physiology paths fail closed under the tested cases.** Swift/Android `HrvWindow` requires accepted original endpoints, consecutive identities and verified timing; filtering a missing/rejected beat cannot simply concatenate its neighbors. The shared `hrv_window_oracle.json` covers `missing_original_interval`, `rejected_shared_beat`, `source_switch`, `legacy_no_identity` and `packet_identity_without_clock_support`. `HrvWindowTests.testActualPacketAdapterDoesNotInventCoverageOrBridgeRemovedWords`, `testConflictingReplayNeverChoosesArrivalOrder` and `testFirmwareSwitchAndMissingProvenanceFailClosed` exercise production adapters. Native/server source readers also exclude suspect canonical inputs. Coarse integer-second packet timestamps remain `continuity_unverified`; a valid CRC/record identity alone does not establish a beat clock. Frozen v1 numerical behavior is preserved separately and is not newly qualified by these tests.

## 5. Can successful scoring continue without exhausting retry state?

**Demonstrated for more than 350 successive revisions, for both independent queues.** Migrations `20260918010000_physiology_revisions.sql`, `20260918100000_physiology_independent_work.sql` and `20260918120000_fenced_baseline_transport.sql` separate success/revision counters from revision-specific failure budgets. `ScoringWorkQueueIntegrationTest.moreThan350SuccessfulRevisionsRemainRunnable` and `failureBudgetBelongsToRevisionAndWaitingDoesNotConsumeIt`, and `IndependentScoringWorkIntegrationTest.moreThan350CompletedBaselineAndShadowRevisionsRemainRunnable`, use actual disposable PostgreSQL. This removes the observed retry-budget exhaustion mechanism; a finite regression is not evidence of an indefinite soak.

## 6. Can stale workers publish or release another worker's lease?

**The tested repaired APIs reject those actions.** Both queues fence publication, renewals and completion/failure/wait by owner/device/day, revision, lease token, run identity and current expiry, evaluated after acquiring the relevant lock. Migration 120000 retires unfenced baseline publication/mutation and introduces the fenced transport for the frozen worker. Tests: `ScoringWorkQueueIntegrationTest.newInputAndExpiredLeaseFenceEveryOldMutation`, `twoWorkersCannotClaimSameLiveRevision`, `leaseThatExpiresWhileWaitingForLockCannotPublishOrRenew`, `nullLeaseArgumentsCannotBypassFence`; `IndependentScoringWorkIntegrationTest.expiredSameInputBaselineWorkerCannotPublishOrReleaseSuccessor` and `newInputFencesBaselineAndLegacyUnpatchedMutationsFailClosed`.

The pre-repair same-input stale baseline completion defect is recorded in `repairs/queue/unfenced-baseline-counterexample.{sql,log}`. Frozen-baseline source preparation proves the patch does not change its numerical kernel or result mapper. This is disposable/local evidence; no deployed worker fleet was exercised.

## 7. Do all physiological dependencies create correct input revisions?

**The enumerated current dependency families have executable coverage; “all” is bounded to the reviewed dependency contract.** Migrations 010000/030000/050000/060000 (full names begin `20260918`) and migration 100000 route durable HR/R-R/packet provenance, respiration, gravity, events/wear state, qualified raw-object availability/revocation, profile/calendar/device-family semantics, annotations and manual boundaries into transactional revision updates. A last-seen heartbeat does not change physiology input. HRV history changes propagate within the declared past-history window and matching owner/device; derived-only refreshes do not endlessly cascade.

Tests: `ScoringWorkQueueIntegrationTest.allAuxiliarySignalsAndManualChangesDirtyWithoutHrArrival`, `deviceFamilyCorrectionInvalidatesButLastSeenDoesNot`, `bulkArrivalCoalescesAndTransactionRollbackCannotDirty`; `PhysiologyDependencyIntegrationTest.eligibilityLossAndScalarChangesInvalidateButArrayOrderDoesNot`, `derivedOnlyRefreshKeepsHistoryAvailableAndDoesNotCascade`; `RrPacketProvenanceIntegrationTest.packetOnlyArrivalRetainsZeroSlotIsOwnerScopedAndDirtiesTransactionally`; `PhysiologyWearDependencyIntegrationTest.delayedOffInsertAndDeleteDirtyExistingDistantDaysTransactionallyButReplayDoesNot`. New future input types must add their own dependency tests.

## 8. Does late data invalidate every affected output?

**Verified for the specified day/night, HRV history, wear and manual-edit cases, including races and calendar travel.** `ScoringWorkQueueIntegrationTest.durableArrivalAndCorrectionCommitWithRevisionAndInvalidateFollowingDay` and `movedManualDetailBoundsInvalidateOldAndNewPeriods` cover direct dependencies. `PhysiologyDependencyIntegrationTest.rawCorrectionCoversEachPriorNightWindowsHistoryButNotDay30`, `earlierArrivalRevokesAnAlreadyRunningLaterLease`, `publicationArrivalAfterLaterCommitStillInvalidatesThatCommit` and `newCalendarCreationAndPublicationCannotMissEachOthersUncommittedRows` cover transitive dependency races. `IndependentScoringWorkIntegrationTest.historicalDependencyUsesOwnedCurrentDateAfterDatelineTravel` checks the repaired frozen-queue-timezone defect; `directLateInputInvalidatesBothButStaleLegacyCompletionCannotTouchShadowLease` checks both versions.

Manual tombstones remain after late generated arrivals: `LegacySleepContinuationIntegrationTest.continuedTombstoneSuppressesLegacyAndLateGeneratedEpisodesAndCanBeRestored`. These deterministic cases cannot establish exhaustive behavior for all possible calendars, firmware or newly introduced features.

## 9. Are user/device/model/version identities isolated?

**Repaired invariants are supported by source checks and disposable-database tests.** Migration 110000 atomically enforces immutable registered owner identity and owner/device references on the specified physiology tables; `_shared/devices.ts` uses `register_noop_device` instead of owner-overwriting upserts. Migrations 100000/120000 isolate baseline and shadow work and heartbeats. Tests: `ScoringWorkQueueIntegrationTest.tenantAndDeviceOwnershipCannotBeForgedAndAuthenticatedCannotOperateQueue`; `PhysiologyPublicationIntegrationTest.corruptOwnerEnvelopeCannotPublish`, `deviceSnapshotsAndManualTombstonesRemainIndependent`, `shadowCannotBeSelectedAndAnotherOwnerCannotReadSnapshots`; `IndependentScoringWorkIntegrationTest.bothWorkersCompleteEitherOrderWithoutConsumingOtherVersion` and `shadowPublicationCannotUseBaselineAlgorithmIdentity`.

`PhysiologyShadowRunnerTest.configured job path executes but cannot switch owner or revision` and native `ServerHrvSeriesTests.testOwnerDeviceRevisionSchemaAndMetricFences` cover downstream identity checks. The lead separately ran full-chain/populated-baseline migrations and defensive ownership/RLS invariants; see `repairs/tenants`. This is not a live authentication/tenant penetration test. Legacy ownership inconsistencies intentionally abort migration rather than being silently rewritten or deleted.

## 10. Do failed archives retry independently?

**Yes in exercised local service/Postgres paths.** `DerivedArchiveOutbox`, `ArchiveRetryWorker`, `DerivedArtifactWriter` and `ScoringApplication --archive-only` separate immutable archive debt from scoring work and give archive completion its own fenced lease. `PhysiologyPublicationIntegrationTest.archiveFailureIsIndependentlyRetryableAndFenced`, `ArchiveRetryWorkerTest.slowArchiveRunsSeparatelyAndDoesNotCreateConcurrentBacklog`, and `failedArchiveDoesNotKillIndependentRetryLane` exercise failures.

Final `IndependentScoringWorkIntegrationTest.baselineArchiveRetriesExactCommittedSnapshotWithoutRescoring` verifies identical retry key/serializer bytes, immutable DB payload equality, rejected stale archive completion, current canonical readback, unchanged queue revision and no new runnable baseline scoring work. The queue auditor reports final service **143/143 passed, including 80 actual-Postgres cases**, in `repairs/queue/final-service-jvm.log`. Real B2 outage recovery/throughput remains untested.

## 11. Does five-minute HRV require real coverage and preserve clean elevated values?

**Verified numerically for synthetic known-clock observations and abstention controls.** `HrvWindow` measures fixed 300-second windows from verified original spans; count/density cannot substitute for observed time. Shared oracle cases include `equal_intervals_true_zero`, `clean_high_variability` (800 ms RMSSD), `sparse_center_island`, `trailing_edge_gap`, `boundary_intervals_excluded_from_sdnn` and `exclusive_right_endpoint`. The audit repaired SDNN's inclusion of boundary-crossing intervals.

`HrvWindowTests.testVerifiedTimingCannotInflateCoverageUsingCoarsePrecision`, `testManyShortIntervalsCannotHideOneUninterpretableLongSpan`, `HrvIntegrationTests.testStageFeaturesUseValidityWithoutSleepLabelAndCannotBypassQuality`, and `testEnginePublishesWindowArithmeticNotCachedVendorValueOrDeepOnlyPreference` exercise the arithmetic and gates. `HrvSeriesTests.testNightlyArithmeticAndRestNapSeparation` and `testDenseEarlyIslandCannotRepresentNight` test aggregation/representativeness. Native readback retains true zero and elevated values. Real WHOOP packet timing/continuous five-minute capture remains unqualified; frequent unavailable outputs can be correct abstention.

## 12. Do Apple SDNN and WHOOP RMSSD remain separate?

**Yes for the new typed window/series/baseline contract; no claim of universal legacy UI separation.** Swift/Android `HrvSeries.sameSeries` compares owner, device, firmware, source, modality, metric, units, algorithm/quality/clock/decoder versions and duration; baseline history is past-only and comparable. `hrv_window_oracle.json` explicitly contains `apple_sdnn_is_not_rmssd`. Native `ServerHrvSeries` requires `metric="rmssd"` and `unit="ms"` and never substitutes a daily scalar/SDNN; both native metric-fence tests cover rejection.

Inherited `Strand/Data/AppleHealthImport.swift:59-67` still mirrors Apple SDNN into legacy generic `avgHrv` and `avgSdnn` under its Apple source. The new series does not consume that generic daily field: service `SignalSampleReader.kt:123-153` loads baseline history from immutable `server_physiology_results.payload.measurements`, matching owner/device/v2 version and current measurement revision, then decodes typed `HrvPayloadCodec` windows. `DayScorer.kt:134` passes these windows into `HrvSeries.baseline`. Preserving frozen v1 math does not certify every historical generic “HRV” display label.

## 13. Can missing sleep data become sleep or a stage?

**The reviewed new paths abstain under tested absence/invalid-input cases.** Swift/Android `SleepSignalValidity` filters invalid HR and zero/nonfinite gravity before feature aggregation/cache identity; missing epochs are unknown. `SleepEvidenceTests.testEmptyAndInteriorMissingEpochsAbstain`, `testInvalidHrAndGravityDoNotCreateEvidenceInEitherStager`, `testInvalidPlaceholdersCannotChangeSparseFeaturesOrCachedResults`, and `SleepOpportunityDetectorTests.testMissingMotionBreaksRunAndDuplicateBurstCannotInventCoverage` cover the repaired zero-HR/invalid-gravity defect. `SleepBoundaryOverridesTest.boundaryWithoutSignalIsUnknownAndReplayDoesNotAccumulate` prevents manual boundaries from manufacturing sleep evidence. These are behavioral evidence, not validation of sleep detection accuracy.

## 14. Are sleep_unstaged and unknown sleep/wake handled correctly?

**Explicit vocabulary/totals and fixture paths distinguish them.** Swift/Android `SleepStageVocabulary` and `SleepStageTotals` retain binary-confirmed sleep as `sleep_unstaged`, while unknown wake/sleep is excluded from known sleep/stage totals. `SleepStageVocabularyTests`, `SleepStageTotalsTests`, `SleepEvidenceTests.testUnknownDayIsUnavailableAndLateShiftSleepUsesExactDayBounds`, and `DayScorerPhysiologyIntegrationTest.groupedInterruptionsReachCanonicalEpochsAndNeverQualifyForSleepPhysiology` cover unknown interruptions.

Actual scorer serialization is consumed by both clients (question 16); that fixture contains 560 `sleep_unstaged` epochs and does not exercise unknown epochs. Separate vocabulary/totals, server grouped-interruption and native sleep-view-model tests cover unknown handling. Manual expansion adds unknown (`SleepBoundaryOverridesTest.movedBoundsClipTruthAndAddUnknownInsteadOfSleep`); deletion/tombstones remain separate from missing signal.

## 15. Do naps and shift sleep work across the full day?

**Full-day deterministic fixtures pass after main-sleep ranking repair.** Swift/Android `SleepOpportunityDetector.mainSleepGroupIndices` qualifies groups by actual known sleep before ranking; an early short nap cannot hide later qualifying shift sleep. Tests: `testTwentyMinuteAfternoonNapAndDaytimeShiftSleepHaveNoTimeOfDayGate`, `testSharedFullDayGroupingOracleQualifiesBeforeRanking`, `testNapsOnlyDayCannotBecomeMainSleepOrNocturnalHrvContext`, `testReadingPhoneUseAndOffBodyAreNotNapsAndStillnessAloneIsUnknown`, `testMainGroupQualificationCountsSleepRatherThanEditedOpportunityDuration` and Android twins. Server `DayScorer` reuses grouping after manual edits.

No full-day labelled human cohort, actual shift-worker recording or device wear/quiet-wake validation was run. “Works” here means implemented/tested timing and ownership behavior, not measured nap sensitivity or specificity.

## 16. Do server stages reach both clients?

**Actual serializer-to-native readback fixtures now cover both clients.** Service `DayScorerPhysiologyIntegrationTest.actualScorerSerializationMatchesBothClientFixtures` calls the actual scorer/publication mapping and checks byte-equivalent checked-in `server_scored_sleep_snapshot.json` copies. Swift store `ServerRespirationSummaryTests.testActualScorerSnapshotSurvivesNativeReadbackAndSQLite` passes the fixture through native parsing and disposable SQLite; Android `push/ServerRespirationSummaryTest.actualScorerSnapshotSurvivesNativeReadback` uses the same payload. They assert owned parsing, two episodes, unstaged epochs, sleep revision 42, retained HRV and the repaired main-sleep respiration summary. The snapshot carries day/device/version and main/nap identities; these two native methods do not independently assert every one of those fields. Unknown epochs are covered by separate tests, not this actual-scorer fixture.

Swift `ServerSleepEpisode`, `ServerSleepViewModelTests`, `Strand/Screens/SleepView.swift`, and Android `push/ServerScoreRepository.kt`, `ui/ServerSleepViewModelTest.kt`/sleep views establish consumption. The fixture uses a faithful readback envelope with synthetic selection metadata; it is not a single live SQL RPC → network → device-render test. UI/device rendering and background cache refresh remain separate gates.

## 17. Can cached results leak across account changes?

**Owner/generation fencing rejects the exercised delayed-request and offline-cache cases.** Swift `Strand/Push/ServerScoreRepository.swift` now has injected live-equivalent dependencies so the actual repository is tested with SQLite. `StrandTests/ServerScoreRepositoryRaceTests` runs late success after sign-out, a delayed unauthorized response after account B starts, overlapping requests finishing out of order, and offline sign-in without restoring account A. The lead executed all four in the macOS app test host.

Android `ServerScoreRepository.kt` uses owner-scoped persistence, generation/request checks and cleared memory; `CloudAuthClient` conditionally clears only the failed session and `SecurePrefs` has no plaintext fallback. Readback DTO memoization was removed from affected screens. Repository/unit evidence does not establish all paused/resumed Compose lifecycle/account-switch behavior: session owner/generation is not itself an observable flow. No emulator/device navigation or account-switch UI run was performed. This remains an explicit device-test gap, not an additional confirmed leak.

## 18. Do respiratory estimates abstain on unsupported or ambiguous signals?

**Verified by positive and negative signal fixtures; reference accuracy is unknown.** Swift/Android `RespirationEstimator` gates known timing/channel, original interval continuity, observed fraction, maximum gaps, modulation/periodicity, harmonics/ACF agreement and enough cycles. The repaired estimator rechecks interpolated/final values against the configured range rather than clipping an out-of-range estimate into it; summary denominators use only the relevant episode context and union overlapping spans.

`RespirationEstimatorTests.testSharedSyntheticOracle`, `testRejectsUnknownTimingAndChannel`, `testCoarseIntervalsDoNotAcquireTiming`, `testRsaClockUncertaintyCannotExpandOriginalRRDurationTolerance`, `testRsaRejectedSharedEndpointCannotContributeObservedSamples`, `testUpsamplingDoesNotInventBandwidthAndFusionDoesNotMultiplyConfidence`, and `testSummaryDistributionRetainsOnlyAcceptedInContextValuesInStableOrder` cover these behaviors. Server integration rejects respiration/HRV from identity-only unproven clocks. No actual respiratory reference dataset or low/high-rate clinical validation was run; supported engineering ranges are not clinical performance claims.

## 19. Are raw waveform objects verified before inference?

**Yes on the implemented guarded path; readiness/HEAD alone is insufficient.** Service `signals/VerifiedRawObjectReader.kt` fetches actual bytes, enforces size/decompression/format limits, checks the correct digest domain and decodes before returning qualified signals. Tests `corruptionSizeOwnerAndTrailingBytesReject`, `decompressLimitRejectsBeforeDecode`, and the identity tests in question 2 cover byte-level behavior. `RawSignalCatalogueIntegrationTest.changedManifestAndCorruptBytesCannotAcquireVerificationProof`, `decodingContractChangedDuringFetchCannotAcquireVerificationProof`, `decodingContractChangesRevokeOldProofAndDirtyItsConsumers`, and `repeatedActualHashAndDecodeDoesNotContinuouslyRedirtyTheQueue` exercise persisted proof/race semantics.

Edge `tests/workers_test.ts` distinguishes append gzip stored-byte hashes from raw NPB1 decoded-content hashes; unsupported zstd reconciliation stays unverified instead of guessing. `SignalInventoryIntegrationTest.RawCatalogueClaimsDoNotBecomeVerifiedCoverageOrPruningPermission` guards availability overclaims. No physical raw capture was independently qualified for wavelength, sensor units or transfer-domain suitability.

## 20. Can shadow models affect canonical outputs?

**The reviewed repaired publication/selection and operational paths isolate them.** Python `physiology_inference/contracts.py` activation/result guards, all eight manifests, JVM `PhysiologyShadowRunner`/`ShadowModelExecutor`, and SQL publication/selection enforce shadow identity and canonical-false. Default selection remains frozen v1. Migration 100000 separates queue/heartbeat ownership; migration 120000 prevents v2 publication under a v1 label and snapshots immutable fenced v1 results. A shadow failure/timeout cannot overwrite the deterministic result or consume baseline work.

Tests include `PhysiologyPublicationIntegrationTest.shadowCannotBeSelectedAndAnotherOwnerCannotReadSnapshots`, `IndependentScoringWorkIntegrationTest.bothWorkersCompleteEitherOrderWithoutConsumingOtherVersion`, `shadowFinishesBeforeBaselineClaimsAndReplayLeavesBaselineCurrent`, `shadowPublicationCannotUseBaselineAlgorithmIdentity`, and `PhysiologyShadowRunnerTest.total deadline preserves deterministic respiration and interrupts model`. Bench `test_gate_simulation_never_enables_publication_and_missing_evidence_fails` makes gate simulation non-promotional. No model promotion or live deployment occurred. Resource isolation on the target Linux VPS and production worker coexistence remain unrun.

## 21. Does preprocessing match pinned checkpoints?

**Partially verified at source/adapter contracts; not all released checkpoints executed.** `physiology_inference/preprocessing.py`/`adapters.py` plus exact source/asset hashes pin the inspected contracts. `test_wav2sleep_exact_target_grid_and_sample_std` checks 1,024 samples per 30 seconds and upstream sample-standard-deviation normalization; gap/wavelength/extrapolation tests reject incompatible data. `test_rr_exact_shape_units_and_alignment` checks RR_Estimation `(2048,4)` at 64 Hz, and `test_rr_does_not_invent_missing_external_preprocessing` requires its unresolved externally prepared contract. Manifest tests independently bind runtime/preprocessing source bytes and distinguish code/checkpoint/data rights.

Actual pinned NeuroKit controls, Walch feature assembly, a synthetic CorrEncoder train/reload and local compact-model subprocess ran. Released wav2sleep weights, RR_Estimation TensorFlow weights with qualified upstream preprocessing, SleepECG released classifiers and RRest/Octave were not all runnable/available in the audited environment. Their missing checkpoint/runtime/rights/transfer evidence is NOT_READY; source inspection and tensor-shape tests are not checkpoint numerical parity.

## 22. Does online inference use future data?

**Known noncausal candidates are rejected in online modes; causal synthetic controls pass.** The repaired `comparators.py` Walch path rejects non-retrospective jobs before imports/asset loading because its pinned feature construction uses centered/future context and global normalization. `test_noncausal_walch_rejects_online_modes_before_importing_or_reading_assets`, `test_correncoder_refuses_causal_or_windowed_waveforms_before_inference`, `test_causal_predictions_are_prefix_invariant`, and `test_gaps_wrong_wavelength_and_causal_extrapolation_reject` cover mode gates.

Native `HrvIntegrationTests.testCausalEngineExcludesIncompleteWindowsAndFutureContext`, `SleepEvidenceTests.testIndependentContextAndCausalFutureInvariance`, and server `DayScorerPhysiologyIntegrationTest.snapshotExcludesFutureSignalsAndUnfinishedMeasurementWindowsWithoutChangingMode` enforce available-at/cutoff behavior. Whole-night shadow models remain explicitly retrospective. This does not prove an unexecuted third-party checkpoint has no hidden noncausal preprocessing.

## 23. Do participant splits or overlapping windows leak data?

**Tested leakage mechanisms are rejected by dataset/training guards.** `Tools/physiology-bench/physiology_bench/{splits,contracts}.py` splits by participant before fitting, binds acquisition identity and checks context overlap/embargo, not merely target-label overlap. Tests in `tests/test_harness.py`: `test_external_people_never_train`, `test_training_fit_rejects_heldout_person`, `test_sequence_context_purged_not_just_output_overlap`, `test_participant_alias_split_is_rejected`, `test_duplicate_source_context_leak_rejected` and `test_fit_audit_rejects_heldout_calibration`.

Repaired `physiology_inference/correncoder.py` binds original source ID and content SHA to one participant and rejects duplicated acquisition windows under aliases; `test_acquisition_aliases_hash_conflicts_and_duplicate_exports_fail_before_training` and `test_one_epoch_reproduction_is_deterministic_and_participant_disjoint` cover it. No real cohort membership/identity audit was possible; deliberately falsified participant metadata beyond recorded acquisition hashes cannot be disproved by a synthetic suite.

## 24. Are scientific claims supported by device/reference validation?

**No device-specific or clinical accuracy/readiness claim is established.** The implementation supplies explicit engineering quality contracts and an evaluation/promotion harness; existing paper/model metrics cited by the spec are not WHOOP-device validation. `test_synthetic_cannot_be_reference_ready_even_when_adjudicated_flag_set`, `test_unverified_real_reference_bytes_cannot_be_ready`, `test_subgroup_report_retains_insufficient_sample_status`, `test_unlabelled_background_does_not_prove_false_episode_rate`, `test_one_participant_has_no_bootstrap_uncertainty_claim`, and `test_missing_all_promotion_evidence_is_not_ready` enforce honest limits.

No audited physical device capture/overnight soak, ECG beat-reference validation, PSG sleep/nap/quiet-wake validation, respiratory reference comparison, real held-out participant accuracy, target-server resource qualification, approved model/data rights package, deployment or canonical promotion was performed. Checkpoint paper accuracy, green repository tests and frozen-baseline byte preservation cannot substitute for those gates. Legacy numerical limitations remain legacy limitations.

## Execution evidence and attribution

- Fresh reviewer independently executed: **56 inference tests**, **64 Edge tests**, **3 portable-Zstandard tests**, **31 focused Swift HRV/respiration/sleep tests**, all passing with no skips; `git diff --check`; frozen-baseline source preparation. Logs and exact commands: `repairs/fresh-review/review.md` and adjacent `inference.log`, `edge.log`, `portable-zstd.log`, `physiology-swift.log`, `frozen-prepare.log`.
- Acquisition auditor executed final full Swift store **597 reported / 596 passed / 1 private-phone-copy skip**, full NoopPush **30 passed**, targeted Android SQLite/migration tests and production-linked promotion/cross-decoder reproductions: `repairs/acquisition/repair-results.md` plus logs/XML.
- Queue auditor executed final service **143/143**, including **80 actual disposable-Postgres cases**, and kernel **806 reported / 801 passed / 5 pre-existing skips**: `repairs/queue/final-service-jvm.log` and associated XML. This update supersedes the fresh-review document's earlier 79-case database gate count; that reviewer did not independently rerun the final DB suite.
- Model/benchmark auditor's full benchmark **64 tests** and inference repair report: `repairs/models/report.md`. Client/sleep area reports: `repairs/clients/report.md`, `repairs/sleep/repair-report.md`. Initial complete read-only pass: `read-only-pass.md`, with area reports linked there.
- Lead owns final Android/app/Swift complete reruns, unchanged-implementation controls, full 85-migration chain, populated-baseline upgrade, ownership mismatch rollback/retry and final commit SHA. Use their exact final logs/results rather than extrapolating from this index. Root reports atomicity/owner invariants and independent Q3 fencing passing as of this index.
- No source changes were made while writing this evidence index. Paths under `repairs/` above are external artifacts relative to `/Volumes/Untitled/physiology-audit/`, not repository files.
